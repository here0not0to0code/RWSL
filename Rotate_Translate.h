#pragma once
#include <vector>

/* #include "glm/glm.hpp"

struct bounding_box
{
	double		length;
	double		width;
	double		hieght;

	glm::dmat4	transformation;
};

bool check_if_point_is_in_bounding_box(glm::dvec3 position, bounding_box* bounding_box_context)
{
	glm::dmat4 inverse_transformation = glm::inverse(bounding_box_context->transformation);
} */